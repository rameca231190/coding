# Tuples is similar to list but they are imutable.

coordinates = (4, 5)
print(coordinates[0])

# You can create a list and have inside a tuples which will be imutable.
coordinates = [(4, 5), (6, 7), (80, 34)]
print(coordinates)